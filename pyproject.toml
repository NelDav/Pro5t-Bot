[project]
name = "pro5t-bot"
version = "0.1.0"
description = "A discord bot for dynamic voice channel creation and management."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "py-cord>=2.5.0",
]
